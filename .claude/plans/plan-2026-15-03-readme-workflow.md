# plan: README にワークフロー全体像を記載する

## Context

現在の README.md は Reddit 収集（collectors）の説明のみで、記事作成の全体フローが記載されていない。実際には以下の 3 ステップのワークフローが存在する：

1. **素材収集** — `python collectors/reddit_collector.py`（手動実行）
2. **記事分析** — `/analyze-articles`（Claude Code スキル）
3. **記事作成パイプライン** — `/article-pipeline`（Claude Code スキル）

この全体像を README.md に追記し、新規参加者やしばらく離れた自分が見ても流れがすぐ分かるようにする。

## 変更内容

### 対象ファイル
- `README.md`（既存を更新）

### 追記する内容

現在の README の「概要」セクションが収集のみの説明になっているので、以下の構成に書き換える：

1. **概要** — プロジェクト全体の目的（Reddit → 分析 → 記事作成）を一文で説明
2. **ワークフロー全体像** — 3 フェーズのフロー図（テキスト）
   - Phase 1: 素材収集（Reddit）— 手動実行
   - Phase 2: 記事分析 — `/analyze-articles` の説明
   - Phase 3: 記事作成パイプライン — `/article-pipeline` の説明（内部ステップも簡潔に）
3. **セットアップ** — 既存のまま維持
4. **使い方** — フェーズごとのコマンドを整理
5. **Claude Code スキル一覧** — 各スキルの一覧表（名前 / 説明 / 用途）
6. **ディレクトリ構成** — 既存を更新（`articles/`, `data/`, `.claude/skills/` を追記）

### 書かない内容
- 各スキルの詳細仕様（それは SKILL.md に書いてある）
- ガイドラインやテンプレートの内容

## 検証方法
- README.md を目視確認し、ワークフローが明確に伝わるか確認
